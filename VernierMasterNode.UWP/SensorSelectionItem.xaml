<UserControl
    x:Class="VernierMasterNode.UWP.SensorSelectionItem"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:VernierMasterNode.UWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid Padding="5 0 0 0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="Auto"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xEB42;" Name="TypeIcon" />

        <TextBlock Grid.Column="1" FontSize="20" Name="SerialNumber" VerticalAlignment="Center" Padding="5 0 0 0"></TextBlock>

        <Button Grid.Column="2" Name="SelectButton" Click="SelectButton_OnClick" Background="{ThemeResource SystemAccentColor}" CornerRadius="5">
            <SymbolIcon Name="StatusIcon" Symbol="Add" Foreground="Black"></SymbolIcon>
        </Button>
    </Grid>
</UserControl>